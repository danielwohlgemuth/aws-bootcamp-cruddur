#! /usr/bin/bash

LABEL="replace-aws-account-id-with-value"

CYAN='\033[1;36m'
NO_COLOR='\033[0m'
printf "${CYAN}== ${LABEL}${NO_COLOR}\n"

placeholder_text="AWS_ACCOUNT_ID""_HERE"

FILES=$(git ls-files)

for file in $FILES; 
do
    sed -i "s/$placeholder_text/$AWS_ACCOUNT_ID/g" $file
done
